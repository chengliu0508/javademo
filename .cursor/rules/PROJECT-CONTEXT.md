# Project Context Rule

## 每次对话开始

请先读取以下文件，并以它们作为“当前项目结构与功能”的事实来源（spec/ 为唯一权威来源）：

- `spec/PROJECT-SPEC.md`

## 每次对话结束

请把 `spec/PROJECT-SPEC.md` 更新为当前项目的最新状态，并同步更新/新增模块文件（`spec/modules/*/*.md`）中对应新增/修改的功能说明。

